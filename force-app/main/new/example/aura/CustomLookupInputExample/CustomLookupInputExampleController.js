// Arquivo: NewRecordFormController.js

({
    doInit: function (component, event, helper)
    {
        helper.setLookupInfo(component, helper);
        helper.searchResults(component, helper);
    },
})