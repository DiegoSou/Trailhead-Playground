```
# Create a project manifest that lists the metadata components you want to deploy or retrieve.

sf project generate manifest --source-dir force-app/main/new --name package-lwcForVFDevs --output-dir manifest
```